<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Nenhuma permissão de internet é declarada — o app não faz, e não
        pode fazer, nenhuma chamada de rede. A gravação de backups em
        Downloads usa MediaStore, que não exige permissão de armazenamento
        no Android 10+ (API 29+).
    -->

    <!--
        Nenhuma permissão de internet é declarada — o app não faz, e não
        pode fazer, nenhuma chamada de rede. A gravação de backups em
        Downloads usa MediaStore no Android 10+ (sem precisar de permissão).
        Em versões anteriores (Android 6 a 9), a permissão abaixo é
        solicitada em tempo de execução apenas para gravar o backup em
        Downloads; ela não é usada para mais nada.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Acompanhamento"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
